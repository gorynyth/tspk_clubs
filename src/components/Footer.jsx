import React from 'react';

const Footer = () => {
    return (
        <footer className={'footer'}>
            <div className="container">
                <span>footer</span>
            </div>
        </footer>
    );
};

export default Footer;
